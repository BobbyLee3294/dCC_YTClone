import React from "react";

const CommentForm = (props) => {
  // create a function that will take the comment state and pass it down to this component to return it in this form
  return {
    /* */
  };
};

export default CommentForm;
